server:
  port: 9000

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/erudio_auth_server
    username: root
    password: admin123
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false